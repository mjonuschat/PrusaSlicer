name: Create flatpak single file bundle

on:
  workflow_dispatch:

jobs:
  bundle_flatpak:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/bundle_flatpak.yml@slic3r3_prototype
    secrets: inherit
    with:
      actions_branch: slic3r3_prototype
