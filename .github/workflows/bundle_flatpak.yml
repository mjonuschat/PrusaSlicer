name: Create flatpak single file bundle

on:
  workflow_dispatch:

jobs:
  bundle_flatpak:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/bundle_flatpak.yml@lm_step_actions
    secrets: inherit
    with:
      actions_branch: lm_step_actions
