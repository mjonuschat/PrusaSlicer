name: Create flatpak single file bundle

on:
  workflow_dispatch:

jobs:
  bundle_flatpak:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/bundle_flatpak.yml@lm_kill_resources
    secrets: inherit
    with:
      actions_branch: lm_kill_resources
