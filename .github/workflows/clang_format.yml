name: Run clang-format

on:
  push:
  workflow_dispatch:

jobs:
  formatting_check:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/clang_format.yml@lm_step_actions
    secrets: inherit
    with:
      actions_branch: lm_step_actions

