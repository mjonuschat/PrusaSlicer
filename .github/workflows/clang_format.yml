name: Run clang-format

on:
  push:
  workflow_dispatch:

jobs:
  formatting_check:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/clang_format.yml@slic3r3_prototype
    secrets: inherit
    with:
      actions_branch: slic3r3_prototype

