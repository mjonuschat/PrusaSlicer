name: Build windows sentry

on:
  push:
    branches:
      - lm_kill_resources
      - release
  workflow_dispatch:

jobs:
  build_windows_sentry:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/build_windows.yml@lm_kill_resources
    secrets: inherit
    with:
      enable_sentry:  true
      release_build: ${{ startsWith(github.ref, 'refs/tags/version_') }}
      actions_branch: lm_kill_resources
      create_pdb: false
      create_installer: false
