name: Build flatpak

on:
  push:
  workflow_dispatch:

jobs:
  build_flatpak:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/build_flatpak.yml@ms_cmake_refactor
    secrets: inherit
    with:
      actions_branch: ms_cmake_refactor
