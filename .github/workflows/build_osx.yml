name: Build osx

on:
  push:
  workflow_dispatch:

jobs:
  build_osx:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/build_osx.yml@ms_cmake_refactor
    secrets: inherit
    with:
      actions_branch: ms_cmake_refactor
