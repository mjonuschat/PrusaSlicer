name: Build osx asan

on:
  push:
    branches:
      - lm_kill_resources
      - release
  workflow_dispatch:

jobs:
  build_osx_asan:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/build_osx.yml@lm_kill_resources
    secrets: inherit
    with:
      with_asan: true
      enable_sentry:  false
      release_build: false
      actions_branch: lm_kill_resources
